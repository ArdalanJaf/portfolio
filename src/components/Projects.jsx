import React from "react";



function Projects() {
  const projects = [
    {
      name: blabla,
      links: {git: blabla, site: blabla},
      notes: blabla,
    }
  ]

  return (
    {projects.forEach((project) => )}
  );
}

export default Project;
